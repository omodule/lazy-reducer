# lazy-reducer
Dynamically load reducer.

## Install
```
npm install --save lazy-reducer
```
## API

#### `<LazyReducer />`

#### `withLazyReducer`

#### `lazyReducerEnhancer`
redux enhancer

## example
[example](./example)
